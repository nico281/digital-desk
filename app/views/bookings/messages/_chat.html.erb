<%= render "shared/chat/chat",
    conversation: booking.conversation,
    mark_read_url: mark_read_conversation_path(booking.conversation),
    form_url: conversation_messages_path(booking.conversation) %>
