<% content_for :title, "Página no encontrada" %>

<div class="min-h-[calc(100vh-64px)] flex items-center justify-center px-4">
  <div class="text-center">
    <h1 class="text-9xl font-bold text-gray-200">404</h1>
    <p class="text-2xl font-semibold text-gray-900 mt-4">Página no encontrada</p>
    <p class="text-gray-500 mt-2">Lo sentimos, la página que buscas no existe.</p>
    <%= link_to "Volver al inicio", root_path, class: "btn-primary mt-6", data: { turbo_action: "advance" } %>
  </div>
</div>
