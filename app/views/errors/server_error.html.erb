<% content_for :title, "Error del servidor" %>

<div class="min-h-[calc(100vh-64px)] flex items-center justify-center px-4">
  <div class="text-center">
    <h1 class="text-9xl font-bold text-gray-200">500</h1>
    <p class="text-2xl font-semibold text-gray-900 mt-4">Algo salió mal</p>
    <p class="text-gray-500 mt-2">Lo sentimos, ha ocurrido un error. Intenta nuevamente más tarde.</p>
    <%= link_to "Volver al inicio", root_path, class: "btn-primary mt-6", data: { turbo_action: "advance" } %>
  </div>
</div>
