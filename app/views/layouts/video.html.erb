<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>Videollamada — D-Desk</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-950 text-white h-full overflow-hidden">
    <%= yield %>
  </body>
</html>
